# Global CLAUDE.md

## Communication Style

- Use British English spelling (colour, behaviour, organisation, etc.)

## Safety

- **Never modify security-sensitive settings** (e.g., Git's `[safe] directory`, SSH config, firewall rules) without explicit user approval — even to work around an error. Prefer asking the user over silently bypassing a security check.
