# CLAUDE.md

The `bashrc` is intentionally monolithic — do not suggest splitting it.

## Conventions

- Commit messages use Conventional Commits: `feat(scope): message`; scope matches the config area
- Co-Authored-By trailer: `Co-Authored-By: Claude (<model>, <effort>) <noreply@anthropic.com>` — use actual model and effort level
- Do not suggest fixing working configurations unless explicitly asked
- When asked to refactor, default to deep structural reorganisation — ask upfront if scope is unclear
